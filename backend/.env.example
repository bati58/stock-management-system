# Server
PORT=4000
NODE_ENV=development

# PostgreSQL
DATABASE_URL=postgresql://postgres:postgresqlpassword@localhost:5432/databasename

# Auth
JWT_SECRET=jwt-secret-token
JWT_EXPIRES_IN=8h

# CORS - the frontend's dev server origin
CORS_ORIGIN=http://localhost:5174
